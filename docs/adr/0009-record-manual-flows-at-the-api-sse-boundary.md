# Record manual flows at the API/SSE boundary

We will add a local or dev-only Manual Flow Recorder at the interview API/SSE boundary rather than recording UI clicks or screen activity. Manual recordings will first land as runtime reports for review and redaction, and only curated outputs will be promoted into repository Replay Traces.
